# ESP32-C3 BLE重量計「Scale」要件定義

## 1. 目的

ESP32-C3を使用したBLE重量計「Scale」のファームウェアを、既存の正常動作を維持しながら拡張可能な構成へ整理する。

基本方針は、既存機能を削除せず、新機能を機能単位で追加し、各機能を個別に有効／無効化できる構成とする。

最終的には、既存機能を含めて機能単位のライブラリへ分割する。

---

## 2. 開発方針

### 2.1 既存動作の維持

- 現在正常動作しているBLE通信・重量測定等を基準実装とする。
- ライブラリ分割や新機能追加によって既存機能を壊さない。
- 大規模な一括変更を避け、機能単位で段階的に変更する。
- 各段階でコンパイル、書き込み、実機動作を確認する。

### 2.2 機能単位の分離

最終構成では、各機能を独立したライブラリ／モジュールとして扱えるようにする。

想定例：

- Scale本体制御
- HX711重量測定
- BLE通信
- 電源制御
- スリープ制御
- 時刻管理
- 設定管理
- データ管理
- ゲートウェイ通信
- 診断・デバッグ

実際の分割単位は、現在のコードを確認したうえで決定する。

### 2.3 機能の個別ON/OFF

各追加機能は、必要に応じてコンパイル時または設定値によって個別に有効／無効化できる構成とする。

ただし、機能OFF時に不要な処理・通信・消費電力が発生しないことを優先する。

---

## 3. ハードウェア前提

### 3.1 MCU

- ESP32-C3
- BLE対応
- Wi-Fiは本Scaleでは基本的に使用しない方向

### 3.2 電源系

電源構成：

- 5V：入力・平滑
- 4.5V：周辺回路用、GPIO制御可能
- 3.3V：ESP32-C3等、常時ON

4.5V系はGPIO10（`ldoen`）でON/OFF制御する。

### 3.3 HX711

HX711を使用してロードセルから重量を取得する。

HX711については、測定終了後に可能な限り低消費電力状態へ移行する。

※ 現在のHX711接続GPIOについて過去資料に複数の記録があるため、最新実機コード／回路図を基準として確定する。

### 3.4 ESP32-C3接続

使用・確認済みの信号：

- EN
- BOOT
- TXD
- RXD
- 3.3V
- GND
- GPIO10：4.5V系LDO制御

その他のGPIOは、回路上の接続状態を確認したうえで扱う。

---

## 4. 重量測定機能

### 必須

- HX711からロードセル値を取得する。
- ゼロ点（Tare）処理を行える。
- 校正値を保存できる。
- 保存した校正値を起動時に読み込める。
- 測定値をBLEで送信できる。
- 測定完了後、不要な回路を停止して省電力化する。

### 校正

将来的に以下の校正方式を利用可能とする。

- ゼロ点校正
- 単一点校正
- 複数点校正
- 校正データのNVS保存
- 校正データの読み出し・消去

---

## 5. BLE通信

### 5.1 基本方針

ScaleはBLEを主要な外部通信手段とする。

Wi-Fiを使用せず、必要なタイミングだけBLEを起動することで消費電力を抑える。

### 5.2 通信方式

想定する通信：

- BLE Advertising
- 必要に応じてBLE接続
- ゲートウェイとのデータ交換
- 時刻同期
- 受信確認

実際にAdvertisingのみで成立する機能と、接続を必要とする機能を分離する。

### 5.3 データ

送信データには、必要に応じて以下を含める。

- Device ID
- シーケンス番号
- 重量値
- 測定状態
- タイムスタンプ
- データ種別
- エラー／ステータス情報

データフォーマットは、後から拡張できるように設計する。

### 5.4 受信確認

ゲートウェイへデータを送信した場合、必要に応じて受信確認（ACK）を取得する。

ACK確認後にスリープへ移行することで、データ未受信による欠落を防止する。

---

## 6. 時刻管理

ESP32-C3単体では長期間の正確な時刻保持を主目的としたRTCとして利用できないため、時刻同期方式を設計する。

### 基本方針

- Wi-FiによるNTP同期をScale本体では原則として行わない。
- BLEゲートウェイから時刻を受信する方式を基本候補とする。
- 同期した時刻を利用して、次回の起動・測定タイミングを決定する。

### 想定フロー

1. Scale起動
2. 必要な場合のみBLEを起動
3. ゲートウェイと通信
4. 時刻を取得
5. 次回起動時刻を決定
6. BLE等を停止
7. Deep Sleep
8. 指定時刻に復帰

---

## 7. 省電力機能

Scaleでは省電力を主要要件とする。

### 7.1 Deep Sleep

測定・通信・必要な処理が終了したらDeep Sleepへ移行する。

### 7.2 不使用回路の停止

使用していないハードウェアを可能な限り明示的に停止する。

対象例：

- BLE
- Wi-Fi
- HX711
- 4.5V系電源
- 不要なタイマー／処理
- 不要なGPIO機能

### 7.3 Wi-Fi

Scale本体ではWi-Fiを使用しないことを基本とする。

目的：

- 消費電力削減
- 不要な無線処理の排除
- BLE通信に処理を集中

---

## 8. ゲートウェイ連携

Scale単体で完結する機能と、BLEゲートウェイを必要とする機能を分離する。

### ゲートウェイの役割

- ScaleからのBLEデータ受信
- Device IDによる識別
- データ受信確認
- 必要に応じた時刻情報提供
- 上位ネットワークへのデータ送信

### Scale側

- 必要なタイミングでBLE通信
- データ送信
- ACK確認
- 必要な時刻情報の取得
- 通信終了後にスリープ

---

## 9. 設定管理

ESP32のNVS（Preferences）等を利用し、設定値を不揮発保存する。

保存候補：

- Device ID
- 校正値
- Tare値
- BLE設定
- ゲートウェイ情報
- 機能ON/OFF設定
- 測定設定
- スリープ設定
- シーケンス番号
- 時刻関連情報

設定項目は将来的な追加を考慮してバージョン管理する。

---

## 10. 診断・デバッグ

開発時にはシリアル経由で以下を確認できる構成を維持する。

想定コマンド例：

- `info`
- `raw`
- `tare`
- `cal`
- `load`
- `clear`

ただし、製品動作時には不要なデバッグ処理を停止または無効化できるようにする。

---

## 11. エラー処理

最低限、以下を識別できるようにする。

- HX711異常
- 重量測定異常
- BLE通信失敗
- ACK未受信
- 設定値異常
- 校正データ異常
- 時刻同期失敗
- 電源制御異常

通信失敗時の再試行回数やスリープへの移行条件は別途決定する。

---

## 12. ソフトウェア構成方針

最終的な構成イメージ：

```text
Scale/
├── Scale.ino
├── ScaleController/
│   ├── ScaleController.h
│   └── ScaleController.cpp
├── Weight/
│   ├── Weight.h
│   └── Weight.cpp
├── HX711Control/
│   ├── HX711Control.h
│   └── HX711Control.cpp
├── BLEControl/
│   ├── BLEControl.h
│   └── BLEControl.cpp
├── PowerControl/
│   ├── PowerControl.h
│   └── PowerControl.cpp
├── SleepControl/
│   ├── SleepControl.h
│   └── SleepControl.cpp
├── TimeControl/
│   ├── TimeControl.h
│   └── TimeControl.cpp
├── Settings/
│   ├── Settings.h
│   └── Settings.cpp
├── Gateway/
│   ├── Gateway.h
│   └── Gateway.cpp
└── Debug/
    ├── Debug.h
    └── Debug.cpp
```

これは最終案であり、現在のコードを確認してから実際の分割を行う。

---

## 13. 開発段階

### Phase 1：現行動作の固定

- 現在正常動作するコードをマスターとして保存
- BLE通信を確認
- 重量測定を確認
- 現在のピン設定を確定
- 現在の電源制御を確認

### Phase 2：省電力化

- Wi-Fi停止
- BLE停止タイミング整理
- HX711停止
- 4.5V系停止
- Deep Sleep導入
- 消費電流測定

### Phase 3：機能分離

- 重量測定処理を分離
- BLE処理を分離
- 電源制御を分離
- スリープ制御を分離
- 設定管理を分離

### Phase 4：通信機能拡張

- Device ID
- シーケンス番号
- データフォーマット
- ACK
- 再送
- ゲートウェイ連携

### Phase 5：時刻管理

- ゲートウェイからの時刻同期
- 次回起動時刻の計算
- Deep Sleepとの連携

### Phase 6：最終整理

- 全機能のライブラリ化
- 各機能のON/OFF
- デバッグ機能の整理
- 消費電力最適化
- 実機での長期動作確認

---

## 14. 非機能要件

### 省電力

最重要要件の一つとする。

「機能を動かす」だけでなく、処理終了後に不要な回路・無線・周辺機器を停止する。

### 信頼性

- 測定データを不用意に失わない。
- 通信失敗を検出する。
- NVSの異常値を検出する。
- 再起動後も必要な設定を復元できる。

### 拡張性

将来的な機能追加で既存機能に影響を与えにくい構造とする。

### 保守性

各機能の責務を明確にし、1つのソースファイルに処理を集中させない。

---

## 15. 現時点で未確定の項目

以下は実装前に確定する。

1. 最新のHX711 GPIO
2. BLE Service UUID / Characteristic UUID
3. BLE Advertisingデータフォーマット
4. BLE接続方式またはAdvertisingのみで実現する範囲
5. Device IDの形式
6. シーケンス番号の仕様
7. ACKフォーマット
8. 通信失敗時の再送回数
9. ゲートウェイとの時刻同期方式
10. 起床・測定・通信の具体的なタイミング
11. NVSに保存する設定項目
12. 各機能のON/OFF方式
13. Deep Sleepからの復帰条件
14. 目標待機電流
15. 目標測定時間・通信時間

---

## 16. 基本原則

本プロジェクトでは、以下を優先順位の高い原則とする。

1. **既存の正常動作を壊さない**
2. **省電力を重視する**
3. **機能を独立させる**
4. **各機能を個別にON/OFFできるようにする**
5. **BLEを基本通信手段とする**
6. **Scale本体ではWi-Fiを原則使用しない**
7. **測定・通信終了後は可能な限り速やかに低消費電力状態へ移行する**
8. **将来の機能追加を前提にデータ形式・ライブラリ構造を設計する**
9. **実機で正常動作を確認した機能だけを次の段階へ進める**
